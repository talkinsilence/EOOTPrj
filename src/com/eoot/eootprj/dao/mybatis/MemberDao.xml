<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.eoot.eootprj.dao.MemberDao">

	<insert id="insert" parameterType="Member">		
		<!-- INSERT INTO MEMBERS(MID, NAME, REGDATE) 
		VALUES(#{mid}, #{name}, GETDATE()) -->
		INSERT INTO MEMBERS(MID, PWD, NAME, BIRTHDAY, PROFILEPIC, REGDATE, FAMCODE)
		VALUES(#{mid}, #{pwd}, #{name}, #{birthday}, #{profilepic}, GETDATE(), #{mid});
	</insert>
	
	<select id="getMember" parameterType="String" resultType="Member">
		SELECT * FROM MEMBERS WHERE MID = #{mid};
	</select>
	
	<select id="getFamMembers" parameterType="String" resultType="Member">
		SELECT * FROM MEMBERS WHERE FAMCODE = #{famcode} ORDER BY BIRTHDAY ASC;
	</select>
</mapper>